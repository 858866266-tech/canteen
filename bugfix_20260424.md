# 智能饭堂 - Bug 修复记录

## 修复时间
2026-04-24 18:50

## 已修复的 Bugs

### 1. showPurchasePreview 变量重名问题 ✅
- **问题**：原代码使用 `const dishes=menu[day]?.[meal]||[]` 声明局部变量，与全局 `dishes` 变量名冲突
- **影响**：导致 `dishes.find()` 无法正确查找菜品对象
- **修复**：将局部变量改名为 `dishNames`

### 2. 导入函数 fetch 兼容性问题 ✅  
- **问题**：直接用 `fetch('广东茂名高州家常菜式大全.md')` 在 file:// 协议下会失败
- **影响**：导入功能在浏览器直接打开 HTML 时无法使用
- **修复**：添加 try-catch 错误处理，失败时使用内置数据

---

## 代码变更位置
- ~/Desktop/智能饭堂/canteen-menu.html
  - 第 1968-1990 行：showPurchasePreview 函数
  - 第 2060-2070 行：importGaoxianRecipes 函数
